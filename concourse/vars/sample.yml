# Sample Concourse vars for wreact-erc20-token pipeline validation.
# Production webhook tokens live in infra/concourse/vars/local.yml (gitignored).

github-access-token:
  username: git
  password: CHANGE_ME

ghcr-credentials:
  username: CHANGE_ME
  password: CHANGE_ME

webhook-token-wreact-erc20-token: CHANGE_ME

kubeconfig-prq: CHANGE_ME

slack-notify:
  secret: CHANGE_ME